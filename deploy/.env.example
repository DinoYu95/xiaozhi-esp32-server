# 复制为 .env 后按需修改：cp .env.example .env
# 仅修改需要改的项即可，未设置的会使用 docker-compose 中的默认值

# ----- MySQL（必改：生产环境务必改密码） -----
MYSQL_ROOT_PASSWORD=123456
MYSQL_DATABASE=xiaozhi_esp32_server
# 暴露 3306 到宿主机，供同机 voiceprint-api 等通过 host.docker.internal 连接
MYSQL_EXPOSE_PORT=3306

# ----- Redis（可选，默认无密码；暴露端口供同机 zhiban-agent 复用） -----
REDIS_PORT=6379
# REDIS_PASSWORD=

# ----- 智控台（manager-api + manager-web） -----
WEB_PORT=8002
# 家长端头像 avatarUrl 前缀（含 /xiaozhi）；不配则依赖镜像内 Nginx 传 Host。HTTPS 或端口特殊时建议写死，如：
# XIAOZHI_PARENT_PUBLIC_BASE_URL=http://47.84.15.224:8002/xiaozhi
# 不填则从源码构建（docker compose up -d --build）；填则拉取该镜像
# WEB_IMAGE=xiaozhi-esp32-server:web_latest
# WEB_IMAGE=ghcr.nju.edu.cn/xinnan-tech/xiaozhi-esp32-server:web_latest

# ----- 小智服务端 -----
SERVER_WS_PORT=8000
SERVER_HTTP_PORT=8003
# 不填则从源码构建（Dockerfile-server-standalone）；填则拉取该镜像
# SERVER_IMAGE=xiaozhi-esp32-server:server_latest
# SERVER_IMAGE=ghcr.nju.edu.cn/xinnan-tech/xiaozhi-esp32-server:server_latest

