# 仅生产环境 ACR 用。复制为 acr.env 后修改（acr.env 勿提交 Git）
# cp acr.env.example acr.env

# 阿里云 ACR（与生产 ECS 同地域）
ACR_REGISTRY=registry.cn-beijing.aliyuncs.com
ACR_NAMESPACE=zhiban

# base：requirements 变更时改 tag 并重建 base
SERVER_BASE_TAG=latest
# app：每次发版改 tag
SERVER_APP_TAG=v1.0.0
